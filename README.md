# Factory session bind

A factory job is queued only after it names a worker id.

## Demo

https://shaneraphel.github.io/factory-session-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
